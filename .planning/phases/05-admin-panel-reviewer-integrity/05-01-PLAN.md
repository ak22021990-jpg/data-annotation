---
phase: "05"
plan: "01"
type: execute
wave: 1
depends_on:
  - "05-00"
files_modified:
  - package.json
  - src/main.jsx
  - src/App.jsx
  - src/hooks/useAnnotationState.js
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-02
user_setup: []

must_haves:
  truths:
    - "Router serves candidate flow at / (with basename /data-annotation/)"
    - "Router serves admin panel at /admin"
    - "Router serves candidate detail at /admin/candidate/:id"
    - "Candidate flow internal navigation (register → tutorial → annotate → feedback → results) works unchanged"
    - "Browser back button works between top-level routes"
  artifacts:
    - path: "package.json"
      provides: "react-router-dom dependency"
      contains: "react-router-dom"
    - path: "src/main.jsx"
      provides: "BrowserRouter wrapper with basename"
      exports: ["BrowserRouter"]
    - path: "src/App.jsx"
      provides: "Route definitions replacing screen-state"
      exports: ["Routes", "Route"]
    - path: "src/hooks/useAnnotationState.js"
      provides: "SCREENS enum removed, candidate flow state preserved"
  key_links:
    - from: "src/main.jsx"
      to: "src/App.jsx"
      via: "BrowserRouter wrapping App"
      pattern: "BrowserRouter.*App"
    - from: "src/App.jsx"
      to: "src/components/*.jsx"
      via: "Route element props"
      pattern: "Route.*element"
---

<objective>
Replace screen-state routing with react-router-dom v7 URL-based routing. Install dependencies, wrap app in BrowserRouter with basename matching vite.config.js base, define three top-level routes, and remove SCREENS enum from useAnnotationState.js.

Purpose: Clean URLs, browser back button, deep linking for admin panel. Foundation for Plans 02-04.
Output: Working router with candidate flow at root, admin routes at /admin and /admin/candidate/:id.
</objective>

<execution_context>
@C:/Users/anoop/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/anoop/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-admin-panel-reviewer-integrity/05-CONTEXT.md
@.planning/phases/05-admin-panel-reviewer-integrity/05-RESEARCH.md
@src/main.jsx
@src/App.jsx
@src/hooks/useAnnotationState.js
@vite.config.js

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/hooks/useAnnotationState.js — SCREENS enum (TO BE REMOVED):
```javascript
export const SCREENS = {
  REGISTER: 'REGISTER',
  TUTORIAL: 'TUTORIAL',
  ANNOTATE: 'ANNOTATE',
  FEEDBACK: 'FEEDBACK',
  RESULTS: 'RESULTS',
  REVIEWER: 'REVIEWER'
};
```

From src/hooks/useAnnotationState.js — return shape (KEEP AS-IS):
```javascript
return {
  screen, setScreen, candidate, scenarioIndex,
  currentSeverity, currentSignals, currentAction,
  setCurrentSeverity, setCurrentAction, toggleSignal,
  answersList, register, startTest, submitAnnotation,
  nextScenario, resetAll, violations, switchedAway,
  ...scoring  // scores, totalPoints, displayScore, band
};
```

From vite.config.js — base path:
```javascript
base: '/data-annotation/'
```
BrowserRouter basename MUST match: `basename="/data-annotation/"`

From src/App.jsx — current screen-state pattern (TO BE REPLACED):
- `if (screen !== SCREENS.ANNOTATE)` → renders non-annotate screens
- `screen === SCREENS.REGISTER` → RegisterScreen
- `screen === SCREENS.TUTORIAL` → TutorialScreen
- `screen === SCREENS.FEEDBACK` → FeedbackScreen
- `screen === SCREENS.RESULTS` → ResultsScreen
- `screen === SCREENS.REVIEWER` → ReviewerScreen
- Default → ANNOTATE two-column layout

From package.json — current dependencies (ADD to):
```json
"dependencies": { "react": "^19.2.0", "react-dom": "^19.2.0" }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install react-router-dom and wrap app in BrowserRouter</name>
  <files>package.json, src/main.jsx</files>
  <read_first>
    - package.json (current dependencies, add react-router-dom)
    - src/main.jsx (current entry point, wrap App with BrowserRouter)
    - vite.config.js (base: '/data-annotation/' — basename must match)
  </read_first>
  <action>
    Run `npm install react-router-dom` to add react-router-dom v7.x to dependencies. Then modify src/main.jsx: import `BrowserRouter` from `react-router`, wrap `<App />` inside `<BrowserRouter basename="/data-annotation/">`. Keep StrictMode wrapper. The basename MUST match vite.config.js `base: '/data-annotation/'` exactly — mismatch causes 404 on /admin in production (per RESEARCH.md Pitfall 1). Do not modify App.jsx yet — that comes in Task 2.
  </action>
  <verify>
    <automated>npm ls react-router-dom</automated>
    <source>src/main.jsx contains `BrowserRouter` and `basename="/data-annotation/"`</source>
  </verify>
  <acceptance_criteria>
    - `npm ls react-router-dom` shows installed version (7.x)
    - src/main.jsx imports `BrowserRouter` from `react-router`
    - src/main.jsx wraps App in `<BrowserRouter basename="/data-annotation/">`
    - `npm run dev` starts without console errors
  </acceptance_criteria>
  <done>react-router-dom installed, BrowserRouter wrapping App with correct basename, dev server runs clean</done>
</task>

<task type="auto">
  <name>Task 2: Replace screen-state in App.jsx with react-router-dom Routes</name>
  <files>src/App.jsx</files>
  <read_first>
    - src/App.jsx (current screen-state logic, all SCREENS references)
    - src/hooks/useAnnotationState.js (SCREENS enum, screen state, setScreen calls)
    - src/main.jsx (BrowserRouter already wrapping App)
  </read_first>
  <action>
    Replace screen-state conditional rendering in App.jsx with react-router-dom declarative routing. Import `Routes`, `Route`, `Navigate` from `react-router`. Define three routes:
    1. `<Route path="/*" element={...} />` — candidate flow (current App JSX content, all screens inline)
    2. `<Route path="/admin" element={<AdminPanelPlaceholder />} />` — placeholder component rendering "Admin Panel — coming soon" (real implementation in Plan 02)
    3. `<Route path="/admin/candidate/:id" element={<CandidateDetailPlaceholder />} />` — placeholder rendering "Candidate Detail — coming soon" (real implementation in Plan 02)
    4. `<Route path="*" element={<Navigate to="/" replace />} />` — catch-all redirect

    Keep the candidate flow inline within the `/*` route element — all current App.jsx content (RegisterScreen, TutorialScreen, AnnotateHeader, EmailDisplay, AnnotationForm, FeedbackScreen, ResultsScreen, ReviewerScreen) stays exactly as-is, just wrapped in the route element. The candidate flow uses internal screen state (useAnnotationState) — this is ROUTE-02: router does not interfere with in-app navigation.

    Remove the `goToReviewer` function and its trigger — reviewer screen will be accessed via route or removed per D-02. Remove `screen === SCREENS.REVIEWER` block from App.jsx.
  </action>
  <verify>
    <automated>npm run build</automated>
    <source>src/App.jsx contains `Routes`, `Route`, `Navigate` imports from `react-router`</source>
    <source>src/App.jsx does NOT contain `SCREENS` references (grep -v '^//' | grep -c 'SCREENS' == 0)</source>
  </verify>
  <acceptance_criteria>
    - src/App.jsx imports `Routes`, `Route`, `Navigate` from `react-router`
    - src/App.jsx has `<Routes>` with at least 3 Route children
    - Route path="/admin" renders a placeholder component
    - Route path="/admin/candidate/:id" renders a placeholder component
    - Route path="/*" renders full candidate flow (all existing screens)
    - No references to `SCREENS` enum remain in App.jsx (excluding comments)
    - `npm run build` succeeds with no errors
  </acceptance_criteria>
  <done>App.jsx uses react-router-dom Routes, candidate flow works at /*, admin placeholders exist at /admin and /admin/candidate/:id, SCREENS enum removed from App.jsx</done>
</task>

<task type="auto">
  <name>Task 3: Remove SCREENS enum and screen state from useAnnotationState.js</name>
  <files>src/hooks/useAnnotationState.js</files>
  <read_first>
    - src/hooks/useAnnotationState.js (SCREENS enum export, screen state, setScreen calls)
    - src/App.jsx (verify no remaining SCREENS imports from useAnnotationState)
  </read_first>
  <action>
    Remove the SCREENS enum export from useAnnotationState.js. The candidate flow now uses internal screen state managed by App.jsx route element — but wait: App.jsx still calls `setScreen(SCREENS.TUTORIAL)`, `setScreen(SCREENS.ANNOTATE)`, etc. from useAnnotationState. These MUST be replaced.

    Replace screen state in useAnnotationState.js with a local string-based screen tracker: `const [screen, setScreen] = useState('register')`. Update all setScreen calls to use lowercase strings: 'register', 'tutorial', 'annotate', 'feedback', 'results'. Remove the REVIEWER screen entirely (accessed via route now). Remove the SCREENS export.

    Keep all other state and callbacks unchanged: candidate, scenarioIndex, answersList, proctorActive, currentSeverity, currentSignals, currentAction, toggleSignal, register, startTest, submitAnnotation, nextScenario, resetAll, violations, scoring.

    Update App.jsx to match: replace `SCREENS.REGISTER` with `'register'`, `SCREENS.TUTORIAL` with `'tutorial'`, etc. in the route element's conditional rendering.
  </action>
  <verify>
    <automated>npm run build</automated>
    <source>src/hooks/useAnnotationState.js does NOT export SCREENS (grep -c 'export.*SCREENS' == 0)</source>
    <source>src/hooks/useAnnotationState.js does NOT reference SCREENS (grep -v '^//' | grep -c 'SCREENS' == 0)</source>
  </verify>
  <acceptance_criteria>
    - src/hooks/useAnnotationState.js has no `SCREENS` export or references
    - screen state uses lowercase strings: 'register', 'tutorial', 'annotate', 'feedback', 'results'
    - All setScreen calls use matching lowercase strings
    - App.jsx conditionals use matching lowercase strings
    - `npm run build` succeeds
    - `npm run dev` — candidate flow works: register → tutorial → annotate → feedback → results
  </acceptance_criteria>
  <done>SCREENS enum fully removed, screen state uses lowercase strings, candidate flow works end-to-end, build passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client→GAS | Admin data fetch crosses to backend — needs passcode auth |
| URL→Route | User navigates to /admin — passcode gate blocks unauthorized access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Elevation of Privilege | /admin route | mitigate | Passcode gate component blocks access without valid passcode (Plan 02) |
| T-05-02 | Information Disclosure | URL params | mitigate | Never pass passcode in URL query params — use component state only |
| T-05-03 | Spoofing | npm install react-router-dom | mitigate | Package verified via npm registry, 13yr age, 12M+/wk downloads (RESEARCH.md) |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
1. `npm run dev` — visit `/data-annotation/` → candidate flow renders
2. `npm run dev` — visit `/data-annotation/admin` → admin placeholder renders
3. `npm run dev` — visit `/data-annotation/admin/candidate/test@email.com` → detail placeholder renders
4. `npm run build` — production build succeeds with no errors
5. Browser back button works between /admin and /
</verification>

<success_criteria>
- react-router-dom v7 installed and functional
- BrowserRouter wraps App with basename="/data-annotation/" matching vite.config.js
- Three routes defined: /* (candidate), /admin (placeholder), /admin/candidate/:id (placeholder)
- SCREENS enum removed from useAnnotationState.js
- Candidate flow works end-to-end via internal screen state
- Build passes, dev server runs clean
</success_criteria>

<output>
Create `.planning/phases/05-admin-panel-reviewer-integrity/05-01-SUMMARY.md` when done
</output>
