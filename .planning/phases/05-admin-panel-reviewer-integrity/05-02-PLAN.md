---
phase: "05"
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/utils/api.js
  - src/hooks/useAdmin.js
  - src/components/AdminPanel.jsx
  - src/components/CandidateDetail.jsx
  - src/components/PasscodeGate.jsx
  - src/App.jsx
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-02
  - ADMIN-03
  - ADMIN-06
  - REV-01
  - REV-02
user_setup: []

must_haves:
  truths:
    - "Admin panel at /admin shows passcode gate before authentication"
    - "Correct passcode (apple-reviewer-2026) unlocks candidate list"
    - "Wrong passcode shows error, does not reveal candidate data"
    - "Candidate list shows name, email, score, band, violations (red when >0), timestamp"
    - "Click candidate row navigates to /admin/candidate/:id with full detail"
    - "Reviewer screen uses same passcode pattern, shows violations column"
  artifacts:
    - path: "src/hooks/useAdmin.js"
      provides: "Admin auth + data fetch hook"
      exports: ["useAdmin"]
    - path: "src/components/PasscodeGate.jsx"
      provides: "Reusable passcode auth wrapper"
      exports: ["PasscodeGate"]
    - path: "src/components/AdminPanel.jsx"
      provides: "Admin list view with passcode gate, sortable table, download buttons"
      exports: ["AdminPanel"]
    - path: "src/components/CandidateDetail.jsx"
      provides: "Individual candidate drill-down with per-scenario breakdown"
      exports: ["CandidateDetail"]
    - path: "src/utils/api.js"
      provides: "fetchAdminCandidates function"
      exports: ["fetchAdminCandidates"]
  key_links:
    - from: "src/components/AdminPanel.jsx"
      to: "src/hooks/useAdmin.js"
      via: "useAdmin hook for auth + data"
      pattern: "useAdmin"
    - from: "src/components/AdminPanel.jsx"
      to: "src/components/CandidateDetail.jsx"
      via: "useNavigate to /admin/candidate/:id"
      pattern: "navigate.*admin/candidate"
    - from: "src/hooks/useAdmin.js"
      to: "src/utils/api.js"
      via: "fetchAdminCandidates call"
      pattern: "fetchAdminCandidates"
---

<objective>
Build admin panel with passcode authentication, candidate list with sortable columns, candidate detail drill-down, and wire existing reviewer screen. Reuse useReviewer passcode pattern, add new GAS endpoint for admin data.

Purpose: Hiring managers can review all candidates, drill into individual reports, with consistent auth pattern.
Output: AdminPanel.jsx, CandidateDetail.jsx, PasscodeGate.jsx, useAdmin.js, fetchAdminCandidates in api.js.
</objective>

<execution_context>
@C:/Users/anoop/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/anoop/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-admin-panel-reviewer-integrity/05-CONTEXT.md
@.planning/phases/05-admin-panel-reviewer-integrity/05-RESEARCH.md
@src/hooks/useReviewer.js
@src/components/ReviewerScreen.jsx
@src/utils/api.js
@src/styles/tokens.js
@src/App.jsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/hooks/useReviewer.js — passcode auth pattern (REUSE):
```javascript
const VALID_PASSCODE = 'apple-reviewer-2026';
// authenticate(passcode) → returns true/false, sets isAuthenticated
// fetchCandidates() → calls fetchReviewerResults(passcode)
// logout() → clears auth state
```

From src/utils/api.js — fetch pattern (EXTEND):
```javascript
export async function fetchReviewerResults(passcode) {
  // GET `${GAS_URL}?action=getResults&passcode=${encodeURIComponent(passcode)}`
  // Returns data.submissions || data, with localStorage fallback
}
```
New function needed: `fetchAdminCandidates(passcode)` — similar pattern but `action=getAdminData`.

From src/components/ReviewerScreen.jsx — table pattern (REFERENCE):
- Sortable columns: name, email, displayScore, band, violations, timestamp
- Violations column: `color: cand.violations > 0 ? '#ff5252' : 'inherit'`
- Sort state: `sortField`, `sortAsc` with handleSort function
- Table class: `breakdown-table reviewer-table`

From src/styles/tokens.js — design tokens (REUSE):
```javascript
export const surface = { ...glass, backdropFilter: 'blur(30px)...' };
export const ACCENT = '#0A84FF';
```

From src/App.jsx — Route placeholders (REPLACE):
```jsx
<Route path="/admin" element={<AdminPanelPlaceholder />} />
<Route path="/admin/candidate/:id" element={<CandidateDetailPlaceholder />} />
```
Replace with real components.

Candidate data shape (from submitResults payload in api.js):
```javascript
{
  timestamp, name, email, totalPoints, displayScore, band, violations,
  elapsedSeconds, severityAcc, signalAcc, actionAcc, perScenario, answers, scores
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PasscodeGate component and useAdmin hook</name>
  <files>src/components/PasscodeGate.jsx, src/hooks/useAdmin.js</files>
  <read_first>
    - src/hooks/useReviewer.js (passcode auth pattern to replicate)
    - src/components/ReviewerScreen.jsx (passcode form UI to reference)
    - src/styles/tokens.js (design tokens for consistent styling)
  </read_first>
  <action>
    Create src/components/PasscodeGate.jsx — reusable passcode auth wrapper component. Props: `{ children, onAuthenticated }`. Internal state: passcode input, error message, isAuthenticated boolean. Uses `VALID_PASSCODE = 'apple-reviewer-2026'` (same as useReviewer.js). On correct passcode: sets isAuthenticated true, calls onAuthenticated callback. On wrong passcode: sets error "Invalid passcode. Access denied." — never reveals children. Style: reuse ReviewerScreen passcode form pattern (form-group, input-field, submit-btn classes).

    Create src/hooks/useAdmin.js — admin data fetch hook. Internal state: candidates array, loading boolean, error string. Export `useAdmin()` returning `{ candidates, loading, error, fetchCandidates }`. `fetchCandidates(passcode)` calls `fetchAdminCandidates(passcode)` from api.js. On success: sets candidates array. On failure: sets error message. Include localStorage fallback pattern from fetchReviewerResults — read from `abuse_test_submission_${email}` keys if GAS fails.
  </action>
  <verify>
    <automated>npm run build</automated>
    <source>src/components/PasscodeGate.jsx exports default function PasscodeGate</source>
    <source>src/hooks/useAdmin.js exports useAdmin function</source>
    <source>src/hooks/useAdmin.js contains VALID_PASSCODE = 'apple-reviewer-2026'</source>
  </verify>
  <acceptance_criteria>
    - PasscodeGate renders passcode input form when not authenticated
    - PasscodeGate renders children only after correct passcode entered
    - Wrong passcode shows "Invalid passcode. Access denied." error
    - useAdmin returns { candidates, loading, error, fetchCandidates }
    - fetchCandidates calls fetchAdminCandidates from api.js
    - localStorage fallback reads abuse_test_submission_* keys when GAS unavailable
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>PasscodeGate component and useAdmin hook created, passcode auth works with apple-reviewer-2026, build passes</done>
</task>

<task type="auto">
  <name>Task 2: Build AdminPanel component with sortable candidate table</name>
  <files>src/components/AdminPanel.jsx, src/utils/api.js, src/App.jsx</files>
  <read_first>
    - src/components/ReviewerScreen.jsx (sortable table pattern to replicate)
    - src/utils/api.js (add fetchAdminCandidates function)
    - src/components/PasscodeGate.jsx (wrap admin content)
    - src/hooks/useAdmin.js (consume hook)
    - src/styles/tokens.js (surface, ACCENT tokens)
    - src/App.jsx (replace AdminPanelPlaceholder route)
  </read_first>
  <action>
    Add `fetchAdminCandidates(passcode)` to src/utils/api.js. Function: GET `${GAS_URL}?action=getAdminData&passcode=${encodeURIComponent(passcode)}`. On success: return JSON data. On failure: localStorage fallback — iterate localStorage keys starting with `abuse_test_submission_`, parse JSON, return sorted by displayScore descending. Same pattern as fetchReviewerResults but returns full submission objects with perScenario, severityAcc, signalAcc, actionAcc, elapsedSeconds fields.

    Create src/components/AdminPanel.jsx. Structure: PasscodeGate wrapper → on authenticated, render candidate list. Fetch candidates via useAdmin hook on mount. Table columns: Name, Email, Score (displayScore% + totalPoints/30), Band (badge), Violations (red text when >0 per D-09), Date Submitted (formatted timestamp), Actions (View Detail button). Sortable by name, displayScore, violations, timestamp — reuse ReviewerScreen sort logic. Each row has "View Detail" button that calls `useNavigate` to `/admin/candidate/${encodeURIComponent(email)}`. Add "Download CSV" and "Download PDF" buttons (placeholders — real implementation in Plan 04). Style: reuse flagmail1 design system — surface cards, breakdown-table class, band-badge classes.

    Update src/App.jsx: replace `<AdminPanelPlaceholder />` with `<AdminPanel />` in the /admin route.
  </action>
  <verify>
    <automated>npm run build</automated>
    <source>src/utils/api.js exports fetchAdminCandidates function</source>
    - `grep -c 'fetchAdminCandidates' src/utils/api.js` >= 2 (definition + export)
    <source>src/components/AdminPanel.jsx contains useNavigate and useAdmin imports</source>
    <source>src/App.jsx imports AdminPanel from './components/AdminPanel'</source>
  </verify>
  <acceptance_criteria>
    - fetchAdminCandidates exported from api.js with GAS GET + localStorage fallback
    - AdminPanel renders PasscodeGate wrapping candidate table
    - Table columns: Name, Email, Score, Band, Violations, Date, Actions
    - Violations column uses red text (#ff5252) when count > 0
    - Table sortable by name, displayScore, violations, timestamp
    - "View Detail" button navigates to /admin/candidate/{encoded-email}
    - Download CSV/PDF buttons present (placeholder text)
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>AdminPanel with sortable candidate table, passcode gate, violation red indicator, detail navigation, build passes</done>
</task>

<task type="auto">
  <name>Task 3: Build CandidateDetail component with per-scenario breakdown</name>
  <files>src/components/CandidateDetail.jsx, src/App.jsx</files>
  <read_first>
    - src/components/ResultsScreen.jsx (per-scenario breakdown table pattern to replicate)
    - src/components/AdminPanel.jsx (candidate data shape, navigation source)
    - src/App.jsx (replace CandidateDetailPlaceholder route)
    - src/styles/tokens.js (surface token)
  </read_first>
  <action>
    Create src/components/CandidateDetail.jsx. Use `useParams` to get `id` (URL-encoded email). Find candidate from AdminPanel's passed data OR re-fetch via useAdmin hook. If candidate not found, show "Candidate not found" message.

    Render candidate header: name, email, score (displayScore%), band badge, violations count, timestamp. Show efficiency stats: severity accuracy %, signal accuracy %, action accuracy %, time efficiency. Render per-scenario breakdown table — reuse ResultsScreen table pattern: columns #, Scenario, Severity (green/red pill), Signals, Action (green/red pill), Points (X/3). Use `candidate.perScenario` array for data — each entry has scenarioTitle, selectedSeverity, correctSeverity, severityPoints, selectedSignals, requiredSignals, signalPoints, selectedAction, correctAction, actionPoints, totalPoints.

    Add "Back to Admin" button using `useNavigate` to `/admin`. Add "Download PDF" and "Download CSV" buttons (placeholders — Plan 04). Style: reuse surface cards, breakdown-table class, severity/action pill colors from ResultsScreen.

    Update src/App.jsx: replace `<CandidateDetailPlaceholder />` with `<CandidateDetail />` in the /admin/candidate/:id route.
  </action>
  <verify>
    <automated>npm run build</automated>
    <source>src/components/CandidateDetail.jsx imports useParams from react-router</source>
    <source>src/components/CandidateDetail.jsx renders per-scenario table with severity/signals/action/points columns</source>
    <source>src/App.jsx imports CandidateDetail from './components/CandidateDetail'</source>
  </verify>
  <acceptance_criteria>
    - CandidateDetail uses useParams to extract candidate id from URL
    - Renders candidate header with name, score, band, violations, timestamp
    - Renders efficiency stats: severityAcc, signalAcc, actionAcc
    - Renders per-scenario breakdown table with 6 columns
    - Severity and action cells use green/red pill styling
    - "Back to Admin" button navigates to /admin
    - Download buttons present (placeholder)
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>CandidateDetail with per-scenario breakdown, efficiency stats, back navigation, build passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client→GAS | Admin data fetch with passcode — passcode sent as query param |
| Passcode→UI | Client-side passcode check — adequate for internal tool, not production auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Information Disclosure | Passcode in URL | mitigate | Passcode stored in component state only, never in URL or query params |
| T-05-05 | Elevation of Privilege | /admin route without auth | mitigate | PasscodeGate blocks children render until authenticated |
| T-05-06 | Spoofing | GAS admin endpoint | mitigate | Admin endpoint requires passcode parameter — same as getResults |
| T-05-07 | Tampering | XSS via candidate name in admin table | mitigate | React auto-escapes text content in JSX — no innerHTML used |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
1. `npm run dev` — visit /data-annotation/admin → passcode gate renders
2. Enter wrong passcode → error "Invalid passcode. Access denied." shown, no candidate data visible
3. Enter `apple-reviewer-2026` → candidate list renders with sortable columns
4. Violations column shows red text when count > 0
5. Click "View Detail" → navigates to /admin/candidate/{email} with per-scenario breakdown
6. Browser back button returns to admin list
7. ReviewerScreen still works with same passcode pattern
</verification>

<success_criteria>
- PasscodeGate component blocks admin access without valid passcode
- useAdmin hook fetches candidates from GAS with localStorage fallback
- AdminPanel shows sortable candidate table with all required columns
- Violations displayed with red indicator when > 0 (D-09)
- CandidateDetail shows per-scenario breakdown with efficiency stats
- Navigation between admin list and candidate detail works with browser back button
- ReviewerScreen continues using same passcode pattern (REV-01, REV-02)
</success_criteria>

<output>
Create `.planning/phases/05-admin-panel-reviewer-integrity/05-02-SUMMARY.md` when done
</output>
