---
phase: "05"
plan: "00"
type: tdd
wave: 0
depends_on: []
files_modified:
  - vitest.config.js
  - src/__tests__/routing.test.js
  - src/__tests__/PasscodeGate.test.js
  - src/__tests__/useProctoring.test.js
  - src/__tests__/csvExport.test.js
  - package.json
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-02
  - ADMIN-06
  - PROCTOR-01
  - ADMIN-05
user_setup: []

must_haves:
  truths:
    - "Vitest configured and runnable via `npm test`"
    - "Route matching tests verify /admin and /admin/candidate/:id resolve"
    - "PasscodeGate rejects wrong passcode, accepts correct"
    - "useProctoring increments violations on visibility change"
    - "downloadCSV produces valid CSV string with escaped quotes"
  artifacts:
    - path: "vitest.config.js"
      provides: "Vitest configuration with jsdom environment"
      exports: ["default"]
    - path: "src/__tests__/routing.test.js"
      provides: "Route matching tests"
      exports: []
    - path: "src/__tests__/PasscodeGate.test.js"
      provides: "PasscodeGate component tests"
      exports: []
    - path: "src/__tests__/useProctoring.test.js"
      provides: "useProctoring hook tests"
      exports: []
    - path: "src/__tests__/csvExport.test.js"
      provides: "CSV export utility tests"
      exports: []
  key_links:
    - from: "vitest.config.js"
      to: "package.json"
      via: `"test": "vitest run"`
      pattern: "vitest"
---

<objective>
Create Wave 0 test infrastructure and test files before any implementation. Set up Vitest config, create route matching tests, PasscodeGate auth tests, useProctoring violation tests, and CSV export tests. All tests run via `npm test`.

Purpose: Nyquist Dimension 8 compliance — test automation exists before implementation. Tests verify behavior, not just compilation.
Output: vitest.config.js, 4 test files, `npm test` runs green (after implementation in Waves 1-3).
</objective>

<execution_context>
@C:/Users/anoop/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/anoop/.config/opencode/get-shit-done/references/tdd.md
@C:/Users/anoop/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-admin-panel-reviewer-integrity/05-CONTEXT.md
@.planning/phases/05-admin-panel-reviewer-integrity/05-RESEARCH.md
@package.json
@src/hooks/useProctoring.js
@vite.config.js

<interfaces>
From RESEARCH.md — Vitest 4.1.7 detected, `npm test` runs `vitest run`. No vitest.config.js exists — need to create one with jsdom environment for component testing.

From RESEARCH.md — react-router-dom v7 provides `createRoutesStub` for testing route configurations without full browser.

From src/hooks/useProctoring.js — returns `{ violations, switchedAway, reset, clearSwitchedAway }`. Tests need to simulate visibilitychange and blur events.

From package.json — current test script: `"test": "vitest run"`. Only existing test: `src/hooks/timer-race.test.js`. Need to add vitest + jsdom as devDependencies if not present.

Candidate data shape for CSV tests:
```javascript
{
  timestamp, name, email, totalPoints, displayScore, band, violations,
  elapsedSeconds, severityAcc, signalAcc, actionAcc, perScenario
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Vitest config and verify test runner</name>
  <files>vitest.config.js, package.json</files>
  <read_first>
    - package.json (check for vitest, jsdom, @testing-library/react devDependencies)
    - vite.config.js (reference for test config — extend or mirror)
  </read_first>
  <action>
    Create vitest.config.js with jsdom environment for component testing. Import defineConfig from vitest/config. Set environment: 'jsdom', globals: true. Include src/**/*.test.js files. Mirror base path from vite.config.js if needed.

    Check package.json for devDependencies: vitest, jsdom, @testing-library/react, @testing-library/jest-dom. If missing, run `npm install -D vitest jsdom @testing-library/react @testing-library/jest-dom`.

    Verify test runner: `npm test` should execute (will find no tests initially, exit 0). After Task 2-4, tests will have assertions.
  </action>
  <verify>
    <automated>npm test</automated>
    <source>vitest.config.js exists and exports config with environment: 'jsdom'</source>
    - `npm ls vitest` shows installed version
    - `npm ls jsdom` shows installed version
  </verify>
  <acceptance_criteria>
    - vitest.config.js exports valid config with jsdom environment
    - vitest, jsdom, @testing-library/react, @testing-library/jest-dom installed as devDependencies
    - `npm test` runs without config errors
    - package.json test script: `"test": "vitest run"`
  </acceptance_criteria>
  <done>Vitest configured with jsdom, test runner works, dependencies installed</done>
</task>

<task type="auto">
  <name>Task 2: Create route matching tests</name>
  <files>src/__tests__/routing.test.js</files>
  <read_first>
    - src/App.jsx (route definitions to test)
    - src/main.jsx (BrowserRouter with basename)
    - vite.config.js (base path: '/data-annotation/')
  </read_first>
  <action>
    Create src/__tests__/routing.test.js. Use `createRoutesStub` from react-router to test route configurations without full browser. Test suite "Route matching" with these tests:

    1. "renders candidate flow at root path /" — stub routes with App.jsx route config, render at "/", assert candidate flow content renders (e.g., RegisterScreen or tutorial content).

    2. "renders admin panel at /admin" — render at "/admin", assert admin panel placeholder or AdminPanel component renders.

    3. "renders candidate detail at /admin/candidate/:id" — render at "/admin/candidate/test%40email.com", assert CandidateDetail component renders with route param accessible.

    4. "catch-all redirects unknown routes to /" — render at "/nonexistent", assert redirect to "/" occurs.

    Use basename "/data-annotation/" in route stub to match production config.
  </action>
  <verify>
    <automated>npm test -- routing.test.js</automated>
    <source>src/__tests__/routing.test.js contains 4 test cases using createRoutesStub</source>
  </verify>
  <acceptance_criteria>
    - routing.test.js has 4 tests: root, /admin, /admin/candidate/:id, catch-all
    - Tests use createRoutesStub from react-router
    - Tests use basename "/data-annotation/"
    - `npm test -- routing.test.js` passes after Plan 01 Tasks 2-3 implementation
  </acceptance_criteria>
  <done>Route matching tests created, 4 test cases covering all routes</done>
</task>

<task type="auto">
  <name>Task 3: Create PasscodeGate and useProctoring tests</name>
  <files>src/__tests__/PasscodeGate.test.js, src/__tests__/useProctoring.test.js</files>
  <read_first>
    - src/components/PasscodeGate.jsx (component to test — created in Plan 02)
    - src/hooks/useProctoring.js (hook to test — existing)
    - src/hooks/useReviewer.js (passcode pattern reference)
  </read_first>
  <action>
    Create src/__tests__/PasscodeGate.test.js. Test suite "PasscodeGate" with these tests:

    1. "renders passcode input when not authenticated" — render PasscodeGate, assert input field and submit button present, children not rendered.

    2. "shows error on wrong passcode" — render PasscodeGate, type wrong passcode, submit, assert "Invalid passcode. Access denied." error shown.

    3. "renders children on correct passcode" — render PasscodeGate with children `<div data-testid="secret">`, type "apple-reviewer-2026", submit, assert children visible.

    4. "does not reveal children before authentication" — render PasscodeGate, assert children NOT in document before auth.

    Create src/__tests__/useProctoring.test.js. Test suite "useProctoring" with these tests:

    1. "starts with 0 violations" — render test component using useProctoring({ active: true }), assert violations = 0.

    2. "increments violations on visibilitychange" — dispatch visibilitychange event with document.hidden = true, assert violations = 1.

    3. "double-fire guard prevents double increment" — dispatch visibilitychange then blur in sequence, assert violations = 1 (not 2).

    4. "clearSwitchedAway resets switchedAway without zeroing violations" — trigger visibilitychange (violations = 1), call clearSwitchedAway, assert switchedAway = false, violations still = 1.

    5. "reset zeros violations" — trigger visibilitychange (violations = 1), call reset(), assert violations = 0.
  </action>
  <verify>
    <automated>npm test -- PasscodeGate.test.js useProctoring.test.js</automated>
    <source>src/__tests__/PasscodeGate.test.js contains 4 test cases</source>
    <source>src/__tests__/useProctoring.test.js contains 5 test cases</source>
  </verify>
  <acceptance_criteria>
    - PasscodeGate.test.js has 4 tests: input render, wrong passcode error, correct passcode success, children hidden
    - useProctoring.test.js has 5 tests: initial state, violation increment, double-fire guard, clearSwitchedAway, reset
    - Tests use @testing-library/react for component rendering
    - Tests dispatch real DOM events for visibilitychange/blur
    - `npm test` passes after Plan 02 Task 1 and Plan 03 Task 2 implementation
  </acceptance_criteria>
  <done>PasscodeGate and useProctoring tests created, 9 total test cases</done>
</task>

<task type="auto">
  <name>Task 4: Create CSV export tests</name>
  <files>src/__tests__/csvExport.test.js</files>
  <read_first>
    - src/utils/csvExport.js (created in Plan 04 — test file created now, tests run after)
  </read_first>
  <action>
    Create src/__tests__/csvExport.test.js. Test suite "CSV Export" with these tests:

    1. "downloadCSV generates valid CSV with headers" — call downloadCSV with test data, assert output starts with header row, contains all data rows.

    2. "downloadCSV escapes double quotes in values" — call with data containing `"` chars, assert doubled (`""`) in output.

    3. "downloadCSV wraps all fields in double quotes" — assert every field wrapped in quotes.

    4. "buildCandidateCSV transforms candidate array" — call with 2 test candidates, assert 2 data rows with correct columns (Name, Email, Score, Band, Violations, etc.).

    5. "buildPerScenarioCSV transforms perScenario array" — call with candidate having 3 perScenario entries, assert 3 rows with scenario columns.

    6. "downloadCSV handles empty array gracefully" — call with [], assert no error, returns early or produces empty output.

    Note: downloadCSV triggers browser download (Blob + anchor click). For testing, extract CSV string generation into a pure `toCSVString(rows)` function and test that directly. downloadCSV wraps toCSVString + Blob creation.
  </action>
  <verify>
    <automated>npm test -- csvExport.test.js</automated>
    <source>src/__tests__/csvExport.test.js contains 6 test cases</source>
  </verify>
  <acceptance_criteria>
    - csvExport.test.js has 6 tests: headers, quote escaping, field wrapping, candidate transform, perScenario transform, empty array
    - csvExport.js exports `toCSVString(rows)` pure function for testable CSV generation
    - `npm test -- csvExport.test.js` passes after Plan 04 Task 1 implementation
  </acceptance_criteria>
  <done>CSV export tests created, 6 test cases covering escaping, transforms, edge cases</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test→Implementation | Tests define expected behavior — implementation must satisfy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
1. `npm test` — all Wave 0 tests run (will fail until implementation complete)
2. After Wave 1-3 implementation: `npm test` — all tests pass green
3. `npm test -- --coverage` — coverage report generated (optional)
</verification>

<success_criteria>
- vitest.config.js configured with jsdom environment
- vitest, jsdom, @testing-library/react installed as devDependencies
- 4 test files created: routing, PasscodeGate, useProctoring, csvExport
- 19 total test cases covering routes, auth, proctoring, CSV export
- `npm test` runs without config errors
- Tests pass after implementation in Waves 1-3
</success_criteria>

<output>
Create `.planning/phases/05-admin-panel-reviewer-integrity/05-00-SUMMARY.md` when done
</output>
