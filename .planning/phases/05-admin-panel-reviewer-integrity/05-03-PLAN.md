---
phase: "05"
plan: "03"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/hooks/useAnnotationState.js
  - src/hooks/useProctoring.js
  - src/utils/api.js
  - src/components/AnnotationForm.jsx
autonomous: true
requirements:
  - PROCTOR-01
  - PROCTOR-02
user_setup: []

must_haves:
  truths:
    - "Tab-switch events increment violation counter during annotation"
    - "Focus-loss events increment violation counter during annotation"
    - "Violations accumulate across all 10 scenarios (not reset between scenarios)"
    - "Violation count included in GAS submission payload"
    - "Proctoring listeners only active during ANNOTATE screen"
    - "switchedAway boolean resets between scenarios but violation COUNT persists"
  artifacts:
    - path: "src/hooks/useAnnotationState.js"
      provides: "Proctoring wired into candidate flow, violations passed to onFinished"
    - path: "src/hooks/useProctoring.js"
      provides: "Existing hook — verify double-fire guard works correctly"
    - path: "src/utils/api.js"
      provides: "submitResults includes violations in payload"
  key_links:
    - from: "src/hooks/useAnnotationState.js"
      to: "src/hooks/useProctoring.js"
      via: "useProctoring({ active: proctorActive }) call"
      pattern: "useProctoring"
    - from: "src/hooks/useAnnotationState.js"
      to: "src/utils/api.js"
      via: "onFinished → submitResults with violations"
      pattern: "violations.*submitResults"
---

<objective>
Wire existing useProctoring hook into candidate flow so violations persist across all 10 scenarios and are included in GAS submission. Verify double-fire guard, ensure proctorActive stays true across scenarios, and confirm violations reach submitResults payload.

Purpose: Proctoring events tracked throughout test, violation count included in submission for reviewer/admin visibility.
Output: Verified proctoring integration, no new files — wiring existing hooks correctly.
</objective>

<execution_context>
@C:/Users/anoop/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/anoop/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-admin-panel-reviewer-integrity/05-CONTEXT.md
@.planning/phases/05-admin-panel-reviewer-integrity/05-RESEARCH.md
@src/hooks/useProctoring.js
@src/hooks/useAnnotationState.js
@src/utils/api.js

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/hooks/useProctoring.js — existing hook (ALREADY BUILT):
```javascript
export function useProctoring({ active = false } = {}) {
  // Returns: { violations: number, switchedAway: boolean, reset: function }
  // Double-fire guard: visibilitychange + blur dedup via lastHiddenRef
  // When active=false: listeners removed, switchedAway reset, violations NOT reset
}
```

From src/hooks/useAnnotationState.js — current proctoring wiring (VERIFY + FIX):
```javascript
const [proctorActive, setProctorActive] = useState(false);
const { violations, switchedAway, reset: resetProctoring } = useProctoring({ active: proctorActive });

// startTest: setProctorActive(true) ✓
// nextScenario (last scenario): setProctorActive(false) ✓
// nextScenario (mid-scenario): does NOT call resetProctoring — switchedAway may stay stale
// resetAll: calls resetProctoring() — resets violations to 0 ✓
// onFinished: passes violations to callback ✓
```

From src/utils/api.js — submitResults payload (ALREADY INCLUDES violations):
```javascript
export async function submitResults({ ..., violations, ... }) {
  const payload = { ..., violations, ... };
  // violations already in payload — no change needed
}
```

Key issue from RESEARCH.md Pitfall 5:
- `switchedAway` boolean should reset between scenarios (call `reset()` or set switchedAway false)
- Violation COUNT must persist (do NOT call `resetProctoring()` between scenarios)
- Current code: `resetProctoring()` only called in `resetAll()` — correct for full reset
- Current code: `switchedAway` NOT reset between scenarios — needs fix
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire useProctoring into useAnnotationState with correct lifecycle</name>
  <files>src/hooks/useAnnotationState.js</files>
  <read_first>
    - src/hooks/useAnnotationState.js (current proctoring wiring, nextScenario, startTest, resetAll)
    - src/hooks/useProctoring.js (hook API: violations, switchedAway, reset)
  </read_first>
  <action>
    Verify and fix proctoring lifecycle in useAnnotationState.js:

    1. **startTest**: Confirm `setProctorActive(true)` is called — it already is. No change needed.

    2. **nextScenario (mid-scenario)**: When advancing to next scenario (scenarioIndex < scenarios.length - 1), the `switchedAway` boolean must reset to prevent stale state, but the violation COUNT must persist. Current code does NOT reset switchedAway. Add `setSwitchedAway(false)` after advancing scenario index. Do NOT call `resetProctoring()` — that would zero the violation count.

    3. **nextScenario (last scenario)**: Confirm `setProctorActive(false)` is called — it already is. This stops listeners. Violations are passed to onFinished callback — already wired.

    4. **resetAll**: Confirm `resetProctoring()` is called — it already is. This zeros violations for fresh test start.

    5. **submitResults**: Verify violations is passed to onFinished → submitResults. Already wired in current code: `onFinished({ ..., violations, ... })` and `submitResults({ ..., violations, ... })`.

    The fix is minimal: add `switchedAway` to the destructured return from useProctoring (already done), and call a switchedAway reset in nextScenario. Since useProctoring doesn't expose a `setSwitchedAway` setter, use the existing `reset()` function BUT that also zeros violations — which is wrong. Instead, modify useProctoring to expose `resetSwitchedAway()` separately, or simply ensure the `active` flag toggle handles it. Per RESEARCH.md Pitfall 5: "Call resetProctoring() in nextScenario" — but that zeros violations. The correct fix: add a `resetSwitchedAwayOnly` function to useProctoring, or handle it in useAnnotationState by tracking switchedAway locally.

    Simplest correct fix: In useProctoring.js, add a `clearSwitchedAway` callback that sets switchedAway false and lastHiddenRef false without touching violations. Export it. Call it from useAnnotationState nextScenario.
  </action>
  <verify>
    <automated>npm run build</automated>
    <source>src/hooks/useProctoring.js exports clearSwitchedAway or equivalent</source>
    <source>src/hooks/useAnnotationState.js calls clearSwitchedAway in nextScenario mid-path</source>
    <source>src/hooks/useAnnotationState.js does NOT call resetProctoring in nextScenario mid-path</source>
  </verify>
  <acceptance_criteria>
    - useProctoring exposes a way to reset switchedAway without zeroing violations
    - useAnnotationState calls this reset in nextScenario when advancing (not last scenario)
    - Violation count persists across all 10 scenarios
    - switchedAway boolean resets between scenarios
    - proctorActive stays true across all scenarios, false after last
    - violations passed to onFinished → submitResults payload
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>Proctoring wired correctly: violations persist across scenarios, switchedAway resets between scenarios, violations in submission payload</done>
</task>

<task type="auto">
  <name>Task 2: Verify proctoring double-fire guard and add visibility indicator</name>
  <files>src/hooks/useProctoring.js, src/components/AnnotationForm.jsx</files>
  <read_first>
    - src/hooks/useProctoring.js (double-fire guard implementation)
    - src/components/AnnotationForm.jsx (add subtle violation indicator if needed)
  </read_first>
  <action>
    Verify useProctoring.js double-fire guard works correctly:
    - `lastHiddenRef` tracks whether we already counted this departure
    - visibilitychange fires first on tab switch → sets lastHiddenRef=true, increments violations
    - blur fires second → checks lastHiddenRef, skips increment (already counted)
    - visibilitychange fires on return → sets lastHiddenRef=false, clears switchedAway
    - focus fires on return → sets lastHiddenRef=false, clears switchedAway

    This logic is correct in current code. No changes needed to useProctoring.js for double-fire.

    For the clearSwitchedAway addition from Task 1: add a `clearSwitchedAway` callback to useProctoring return:
    ```javascript
    const clearSwitchedAway = useCallback(() => {
      setSwitchedAway(false);
      lastHiddenRef.current = false;
    }, []);
    ```
    Export it alongside violations, switchedAway, reset.

    No UI indicator needed for violations during the test — violations are invisible to the candidate (per design). The count is only visible to reviewers/admins.
  </action>
  <verify>
    <automated>npm run build</automated>
    <source>src/hooks/useProctoring.js returns clearSwitchedAway function</source>
    <source>Double-fire guard logic unchanged (lastHiddenRef pattern intact)</source>
  </verify>
  <acceptance_criteria>
    - useProctoring returns { violations, switchedAway, reset, clearSwitchedAway }
    - clearSwitchedAway sets switchedAway false and lastHiddenRef false without touching violations count
    - Double-fire guard still prevents counting same departure twice
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>clearSwitchedAway added to useProctoring, double-fire guard verified, build passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser→Candidate | Proctoring runs client-side — candidate could disable JS to bypass |
| Client→GAS | Violation count sent in submission payload — could be tampered |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Tampering | Client-side violation counting | accept | Client-side proctoring is deterrent, not forensic — adequate for internal assessment |
| T-05-09 | Repudiation | Candidate denies tab switching | accept | Violation count is logged in submission, visible to reviewer — sufficient for hiring context |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
1. Manual test: Start test, switch tabs once, return → violations = 1
2. Switch tabs again → violations = 2 (not 4, double-fire guard works)
3. Advance to next scenario → violations still = 2 (count persists), switchedAway = false
4. Complete all 10 scenarios → violations count in localStorage submission matches manual count
5. Check localStorage: `abuse_test_submission_{email}` contains `violations` field with correct count
</verification>

<success_criteria>
- useProctoring hook returns clearSwitchedAway function
- Violations accumulate across all 10 scenarios without reset
- switchedAway boolean resets between scenarios
- Violation count included in GAS submission payload
- Double-fire guard prevents counting same departure twice
- No visible proctoring UI during test (candidate-facing)
</success_criteria>

<output>
Create `.planning/phases/05-admin-panel-reviewer-integrity/05-03-SUMMARY.md` when done
</output>
